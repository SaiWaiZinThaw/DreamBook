import Menu from "@/components/Menu";
import { NavBar } from "@/components";

const Nav = () => {
  return (
    <div className="w-full h-full ">
      <Menu />
      <NavBar />
    </div>
  );
};

export default Nav;
